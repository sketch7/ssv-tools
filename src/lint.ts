import {
	// ts
	lintTs,
	LintTsOptions,
	LintTsResult,
	LINT_TS_FORMATTERS,
	LintTsFormatters,

	// html
	lintHtml,
	LintHtmlOptions,
	HtmlLintResult,

	// sass
	lintSass,
	LintSassOptions,
	LINT_SASS_FORMATTERS,
	LintSassFormatters,
} from "@speedy/build-tools";

export {
	// ts
	lintTs,
	LintTsOptions,
	LintTsResult,
	LINT_TS_FORMATTERS,
	LintTsFormatters,

	// html
	lintHtml,
	LintHtmlOptions,
	HtmlLintResult,

	// sass
	lintSass,
	LintSassOptions,
	LINT_SASS_FORMATTERS,
	LintSassFormatters,
}